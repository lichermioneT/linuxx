硬件
内核
shell命令解释器
外层应用层

shell解释命令行解释器
实际上Windows的图形界面就是win的shell

shell是linux的编程语言
shell可以直接调用linux的命令

linux的基本shell是bash

3历史命令
history -c 清楚历史命令
history -w 马上写入当前指令

/etc/profile

文件和指令都可以用tab键补全的

alias 别名='原名'
alias


ctrl + c
ctrl + l

// 删除剪切
ctrl + u
ctrl + y

ctrl + d退出当前终端。


标准输入输入出
/dev/stdin  0
/dev/stdout 1
/dev/stderr 2


指令 > 文件
指令 >> 文件

指令 2> 文件
指令 2>> 文件


指令 &>> 文件
指令 &> 文件

丢到垃圾箱
ls &> /dev/null

正确在一个文件，错误在一个文件
ls >> f1 2>> f2

输入重定向
wc 行 字符 行数


命令1; 命令2;....
dd 磁盘拷贝文件

指令1 && 指令2
指令1正确，指令二才能够执行

指令 && echo yes || echo no



管道符：
命令1 | 命令2
命令一的输出作为命令的操作对象


grep -i
grep -n
grep -v


用来匹配文件名字的
?任意一个字符
*任意一个或者零个字符
[abc],任意一个a, b, c。
[a-z],任意一个a, b, c,.....z。
[^0-9]

ls [0-9]abc
ls [^0-9]abc
---------------
？
*
[]
[^]
----------------
'' 里面内容原样输出
"" 特殊内容特殊输出 相当于格式输出
  abc="&(date)"
  abc=`abc`
$()
#
$
\ 转义字符


自定义变量
  变量不能数字开头
  变量默认都是字符串类型
  左右两边没有空格
  name=$(date)

自定义变量
  本地变量
  局部变量
"&aa"123
${aa}123

环境变量
  全局变量
  pstree
  PATH
  变量叠加
  PATH="$PATH":/root/sh

位置参数变量
预定义变量












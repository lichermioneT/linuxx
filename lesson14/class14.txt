父进程
子进程：处于僵尸状态，为了让自己状态被父进程知道。
  
孤儿进程。操作系统的一号进程领养。
孤儿进程会变成后台进程。
  
优先级
  pri 80
  ni [-20, 19]

进程获得资源的先后顺序
  优先级：资源的有限性。

cpu里面的大量寄存器
上下文保护
上下文恢复

进程切换

并行：多cpu执行多个进程
并发:进程切换。看起来所有的进程都在执行。(大量上下文的切换和上下文恢复)
时间片
进程的临时数据保存着PCB里面和运行队列里面。

CPU的功能
取指令
分析指令
执行指令

c/c++代码----->编译链接----->CPU的指令

PATH
是可执行指令的路径

shell也是一个进程。

env
printenv

echo $PATH
echo $PWD
echo $LOGNAME
echo $HOME

su - 完全切换，环境变量也切换了
su 只是切换身份，环境变量没有换。

user:环境变量的意义，可以表示当前那个用户在使用Linux

myval=1234

c语言指针失败都是NULL
c语言指针


export设置一个全局变量

环境变量具有全局属性，是会被子进程全部继承下去的。(为了不同的引用场景)(帮我身份认证，找指令的运行路径)(pwd, whoami 这也是程序)
bash是一个进程
./a.out是bash的一个子进程。

本地变量只能在当前bash有效的，不能被子进程继承下去的。

set 指令
unset 指令

ls 怎么知道当前路径呢？
pwd

shell会自动调节PWD

OS
操作系统



用户
系统调用接口
操作系统(进程控制，文件系统，内存管理，驱动管理)
驱动 
硬件


管理的本质是对被管理者的数据做管理。
管理者，对重大事有决策的能力。


管理者：  做决策                                                           校长      OS(操作系统)
执行者：  做执行 1.执行上面的命令 2.和被管理者接触，拿到相应的数据。       辅导员    驱动 (磁盘驱动，显示器驱动) 软件
被管理者：行动产生数据                                                     学生      硬件 (磁盘，网卡， 显卡，键盘，显示器)


管理的本质是对被管理者的数据做管理。
学生的信息太多了，我所管理的学生信息种类是一样的。

被管理对象进行建模的过程！
struct stu
{
  name;
  sex;
  addess;
  telephone;
  qq;
  score;
  struct stu* next;
};

struct stu zhangsan = {};
struct stu lisi = {};
struct stu wangwu = {};

转换成对链表的管理
head-->stu1-->stu2-->stu3-->null

先描述:抽象数据
在组织:数据结构 

所有的管理的本质，先描述在组织。
1.通讯录管理 a.先构建结构体 b.数据结构组织。
2.先写一个类(先描述)，list, vector(组织)


struct dev
{
  struct dev* next;
  int type;
  int status;
  ......
};

管理的本质：对数据做管理    语言方面
管理的方法：先描述，在组织  数据结构

对软件呢？
  一模一样的。

软件能够管理软件
软件也能够管理硬件

操作系统不相信任何人。
  1.保护自己       不让任何人直接进入操作系统，
  2.对外提供服务   操作系统接口


计算机的软硬件体系结构
用户
系统调用接口(系统调用)  (本质上就是c式的接口)  (man syscalls) (strace ls)
(shell(指令操作)/lib(c/c++)(编程操作)/界面)
操作系统(进程控制，文件系统，内存管理，驱动管理)
驱动 
硬件

shell(指令操作)/lib(c/c++)(编程操作)/界面

1.冯诺依曼
2.操作系统本身的理解
3.计算机体系结构

什么是进程？
  一个运行起来(加载到内存)的程序---进程
  在内存中的程序的-----------------进程
  


  CPU-----------内存-------------------磁盘
  
写好的程序本质就是文件，在磁盘上放着。
  那么多的程序，操作系统如何管理呢？
    PCB(process control black)进程控制块
    struct task_struct
    {
      // 该进程的所有属性
      // 该进程对应的代码和属性地址
      
      struct task_strunct* next;
    }

  
  struct task_struct* p1 = (struct task_struct*)malloc(sizeof(struct task_struct));
  p1->...xx
  p1->add = 代码和地址
  
每一个进程一个进程控制块
然后链接起来
  
  所谓对进程进行管，变成了对进程对应的PCB进行相关的管理。
  对进程的管理-->对链表的增删查改

  struct task_struct 内核结构体 --> 内核结构体的对象 ---> 将该结构体和你的代码和数据关联起来----> 先描述，后组织。
  
  进程 == 内核数据结构 + 进程对应的磁盘代码

为什么会有PCB(struct task_struct)的结构体呢？
 进制在调度运行的时候，就具有动态属性。

见见与进程有关的系统调用
  ps axj
  head -1;
  grep 
  
ps(process status)
-a:显示全部进程不仅仅当前用户的
x：列出所有进程，包括那些没有控制终端的（通常是后台守护进程）。
u：采用面向用户的格式（User-oriented），提供更详细的用户、CPU、内存等信息。
j:以作业控制格式显示






























































































今日目标：
1.复习
2.网络拥塞
3.listen的第二个参数
4.进入下一层ip，作用，理解，核心报头字段。

1.复习
  网络基础1，网络分层，自顶向下封装，自底向上解包。
  协议如何决定封装，协议如何进行解包，交付。
  网络版本的计算器。序列化和反序列化。
  协议就是一种约定。
  HTTP协议
  HTTP协议的格式。
  cookie和session。
  HTTPS对称加密，非对称加密，数据指纹，数据摘要，公钥，私钥。CA认证。中间人截取。

  UDP协议。
  1.报头有效载荷如何交付，Linux报头本质就是一种数据类型struct，或者一种位段结构。

  TCP协议
  1.可靠。这里的可靠与不可靠，相对而言。不可靠简单方便，可靠复杂维护成本高。
  2.16位源和目的端口。32序号和确认序号。
  3.16位窗口大小，同步双方的接收能力。 TCP是全双工的。接收缓冲区和发送缓冲区。剩余空间的大小就是接收能力。
  4.三次握手，四次挥手。 TIME_WAIT。
  5.六个标记位。URG ACK PSH PST SYN FIN。标记位的本质，TCP报文的不同类型。进行不同的处理。
  6.TCP是传数据可靠，三次握手和四次挥手不一定会成功。
  7.丢包应答，超时重传机制。
  8.滑动窗口机制。对方的接收能力范围了，尽可能的快。
  滑动窗口 char buffer[N] 一个环形队列。
    1.发送已经确认
    2.发送没有确认
    3.没有发送的   
      分成三个区域。
    实现数据并发，效率更高。

流量控制：怕对方吃不消
拥塞控制：怕网络吃不消

2.网络拥塞
2.拥塞控制
  1.发送10000，丢失1个    自己的问题。
  2.发送10000，丢失9999个 网络的问题。  超时重传吗？不应该的。已经出现问题的网络，又会出现大量的报文，只会加重网络的故障问题。网络里面不仅仅只有自己，大家重传，那不寄了。
  TCP不仅仅考虑了双方的问题，还考虑了网络的问题。
  3.不应该大量传。
  
  TCP引入慢启动机制。
  拥塞窗口（窗口就是一个下标）
  
  我：滑动窗口
  网络：拥塞窗口、
  对端：窗口大小(自己的接收能力)
  我--》网络---》对端==》 滑动窗口大小==min(拥塞窗口，窗口大小(自己的接收能力))
  
  慢启动。指数级增长，超过窗口，是线性的。
  TCP的拥塞控制非常优雅。
  TCP的拥塞控制，可靠性和高效率。

延迟应答
C                 S
      发送data--> 
        <--ack返回 等待一会儿。数据被拿了，缓冲区的数据变大了。
增加网络吞吐量。
虽然也会产生网络拥塞情况，这一般不会的，小概率情况。
目标是在网络不拥塞的情况下尽量提高传输效率。
延迟应答策略
数量限制：每个N个限制                N一般是2
时间显示：超过最大时间就延迟应答一次 TIME 200ms

捎带应答
确认数据的应答（比较常见）


面向字节流。
TCP的发送缓冲区
TCP的接收缓冲区
OS的发送和接收函数都是拷贝函数，拷贝到上面的缓冲区里面。

TCP看来只是字节数据。应用层关心这个数据。序列反序列化。
UDP发一个，你就接收一个UDP报文。

粘包问题
报文没有分开，第一个没有读完，影响第二个。
如何解决
明确两个包之间的边界。应用层自己在做。

关机时正在运行的进程也要被关闭。

拔网线，短电源。

TCP小结
TCP可靠又高性能
可靠
  校验和
  序列号
  确认应答
  超时重发
  流量控制
  连接管理
  拥塞控制

性能
  滑动窗口
  快速重传
  延迟应答
  捎带应答
其它
  定时器

基于TCP的协议
http
https
ssh

TCP和UDP协议没有好坏，只有合适场景。

Listen的第二个参数。
  海底捞不能让客户等的时候没有取号。
  资源百分之百被使用。不会出现空闲的情况。
  1.排队的本质是让我们紫资源空间的时候，可以立马使用，可以提高效率。
  2.不能不排队，不能让队列太长。太长没有必要 a.客户不能忍，b.为什么不把多出来的前，用来更改场地
  生活中提炼出来的核心理念。
    
  服务器进程 海底捞
  listen---->服务员
  tcp协议，要为上层维护一个链接队列
  1.不能没有
  2.不能太长
  队列的长度受listen的第二个参数的影响。
  全链接队列
  TCP底层最多有 backlog+1个 完整链接。
  后续来的都是半链接。如果没有尽快完成三次握手，自动被server关掉。
  环形队列，就是缓冲区。


半链接状态，三次握手还没有成功的。


总结
  应用层
  传输层

























secure shell:ssh

epoll底层维护的红黑树的key是fd.

epoll原理：有事件就就绪了就添加到就绪队列里面
1.什么叫做事件就绪你呢？
 底层的IO条件满足了，可以进行某种IO行为了，就叫做事件就绪。 

select/poll/epoll/ 等是手段--》IO就绪事件的通知机制。 通知机制，有没有策略呢？

打电话派发快递的本质: 就是通知事件到来了！！

边缘触发：
水平触发：

特性,水平触发 (LT),边缘触发 (ET)
默认状态,是,否（需显式设置 EPOLLET）
通知频率,只要有数据，每次 wait 都会通知,仅在数据到达瞬间通知一次
编程难度,较低，逻辑简单,较高，需处理 EAGAIN 和循环读写
性能,一般（存在冗余提醒）,极高（高并发场景首选）
IO 模式,阻塞 / 非阻塞均可,必须使用非阻塞 IO
代表案例,Redis,"Nginx, Go 运行时"

水平触发 (Level Triggered, LT)  —— 默认 一直通知你
边缘触发 (Edge  Triggered, ET)  —— 高速模  我只通知你一次
ET有自己的价值。

1.不仅仅体现在通知机制上
2.尽快让上层把数据读走 TCP可以给发送方提供一个更大的窗口，让对方更新出更大的滑动窗口，提供底层的数据发送效率，更好的理由诸如TCP延迟应答策略！
TCP当中的PSH的作用？？让底层的数据就绪，再让上层知道！

1.基于ET模式下的reactor，处理所有的IO。
  a简洁版的reactor
    半同步半异步IO
    linux网络中，最常用的，最频繁的一种网络IO设计模式。


局域网数据报文都收到，不要的主机直接丢弃了。
局域网就是一个碰撞域。
共享资源，局域网。
临界资源。

交换机，正确的数据丢弃，减小碰撞域。

数据量太长了，尽管光速很快，也是容易发生碰撞的。
MAC帧有最大帧的规定，这就是为什么。
虽然解耦，上下层的关系，不一定解耦特别好的。

IP地址 起点和终点
MAC地址 区间的起点和终点

主机交付给下一跳(下一跳的主机还是路由器等其它节点)
前提一定是 该数据帧被路由过，因为网络层在链路层的上层

MTU

路上的节点会不会对数据分片呢？
  有可能的
大家用的都是TCP/IP协议。
  你的MTU是1500,它的是500.可能出现分片的。

IPA---IPB并不是只有一条路可以选择的。
静止分片标志位--淘汰带宽比较低的路径。
大数据量比较重要的MTU

应用层  有效载荷1460            所以滑动窗口，范围内为什么要有多个报文段？？不允许一次发送太大的单个数据段 MSS message segment size
TCP/UDP tcp报头+有效载荷 = 1480
IP      报头+有效载荷 = 1500 
MAC



ARP协议

跨网络实际上是一个一个局网的不断转发。因为路由器的存在。
路由器子网的选择，不断淘汰的。

你要去的主机就在当前网络中
封装成MAC帧，发送给目标主机
我们必须知道目标主机的MAC地址

还需要有一个过程，让路由器设备，认识主机，获取主机的MAC地址。

ARP协议把IP地址，转成MAC地址，获取对方主机的MAC地址之后，在发送MAC帧。
  根据IP得到目标主机的MAC地址。

ARP协议的故事
  10---张三
  11---lic

主机1  主机2  主机3  主机4  主机5
    主机6 主机7 主机8 主机9

应用层数据报
IP层
ARP
MAC帧

  



1广播一条消息 谁是主机5 MAC地址，IP地址
1.ARP请求阶段，先做群内广播
2.ARP应答，目标主机1V1应答

ARP协议其实是工作在数据链路层的协议。

ARP报文格式
前面数以太网首部 

IP--->MAC帧的能力
硬件类型 协议类型  硬件地址长度 协议地址长度 op 发送端以太网地址 发送端IP地址 目的以太网地址 目的IP地址。
MAC帧 硬件类型 协议类型  硬件地址长度 协议地址长度 op 发送端以太网地址 发送端IP地址 目的以太网地址 目的IP地址。
发送到网络里面。
  目标地址是FFFF，全部机器都需要处理的。交付给ARP层。
  首先识别目的IP，

1.可能受到ARP请求
2.曾经向别人发起过ARP请求，所以别人可能会给主机9，进行ARP应答
a.别人的请求
b.别人的应答
怎么知道是a.还是b.呢？
  先看op
  1是请求
  2应答
  在看目的IP

  先看op
  1是请求
  2应答
  在看目的IP

通信之前先
ARP建立，获取MAC地址。

主机1 --- 路由器1的MAC地址 --- 路由器2的MAC地址 ---...---- 主机9
路由器也要封装获取MAC地址，一直在进行的。

ARP是会缓存的




应用层
传输层
ip层
数据链路层 路由器

RARP逆地址解析


ARP收到应答的时候，会以最新的为主！





















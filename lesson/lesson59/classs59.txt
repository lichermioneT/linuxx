复习
ip协议的基本理解
报头和有效载荷分离
向上交付
子网划分
全球就是一个大大的子网团

大区域
小区域
小小区域
路由器转发

NAT技术

路由器搭建局域网
数据推送到路由器
路由器局域网--》层层交付。
LAN和WAN口IP的层层替换

路由
记住告诉别人，从哪儿来到哪儿去

目的地址
目标网络--》目标主机

当IP数据包, 到达路由器时, 路由器会先查看目的IP;
路由器决定这个数据包是能直接发送给目标主机, 还是需要发送给下一个路由器;
依次反复, 一直到达目标IP地址;



路由器记住网络号
route

目标地址
1.遍历路由表
2.目的IP&路由表中配置的GenMask,确定改报文要去的目标网络
3.对比结构 和 Destination
4.发送给eh0

特殊情况
default地址 扔给默认网关。

我们要去的目标网络号会不会发送变化？会的

数据不断传输
子网掩码越来越大
要去的网络越来越具体
网络号越来越长，主机号越来越短。

路由表生成算法


ip报头
16位总长度 3位标志位 13位片偏移
路由器之间ip报头
网线上跑的MAC帧
数据链路层，MAC帧协议，自己的有效载荷不能超过1500字节 (MTU 可以修改的)
单个报文不要超过1500---》MAC帧的有效载荷
  1500 ip报头+ip报文的有效载荷

IP能 决定单个报文的大小呢？ 不能的
  在网络中决定单个报文大小的是TCP(后面讲)

IP仅仅是一个转发的，路由的功能。


IP的分片与组装，不是主流情况！
分片与组装的字段
16位总长度 3位标志位 13位片偏移

分，自己的IP层
组，对端的IP层

MAC帧只关心自己的报文，超过规定没得。
TCP不关心报文的大小，真的关心就不发给你的
分片和组装是IP层纯纯的自己的工作。
纯纯的和TCP协议和MAC帧解耦的工作

TCP
IP
MAC

tcp报文。你如何拆了人家如何组装。
0.你怎么知道一个报文分片了？？
1.同一个报文的分片都能够被识别出来
2.那一个是第一个，哪一个是最后一个。有没有全收到。
  收全数据
3.那个在前面，那个在后面，如何进行正确的组装
  合并成功，形成一个报文。
4.怎么保证我合起来的报文是正确的！


字段	长度	功能
标识(Identification)	16位	唯一标识一个原始数据包，所有分片共享相同ID

标志(Flags)	3位	包含3个标志位：
• 保留位(Reserved, 0)
• 不分片(DF, Don't Fragment)
• 更多分片(MF, More Fragments)

片偏移(Fragment Offset)	13位	指示当前分片在原始数据包中的位置（以8字节为单位）

每一个分片的报文，报头重新封装ip报头。
也是一个独立的报文。
更多分片位是0，最后一个分片报文。


0.你怎么知道一个报文分片了？？
  更多分片标志位是一，就是分片了
    如果是接收分片最后一个呢？

1.同一个报文的分片都能够被识别出来
    如果更分片是零&&片偏移>0 说明是分片，否则不是！
2.那一个是第一个，哪一个是最后一个。有没有全收到。
  更多分片是1，片偏移是零
  更多分片：0 偏移量>0
  当前起始位置+自身长度=下一个报文中填充的偏移量大小
3.那个在前面，那个在后面，如何进行正确的组装
  合并成功，形成一个报文。
  只需要按照偏移进行升序排序即可
4.怎么保证我合起来的报文是正确的！


1.分片好吗？
  优点：特别不好。就是不好！不是主流情况。
2.对tcp和udp包括ip本身有什么影响吗？
  对tcp和udp的角度，只发了一个，只是接受 一个。
  一个报文被拆成两个或者多个。任意丢失一个报文，就会造成拼接组装失败。直接重发一个。
  重传
  分片增加丢包概率


自己试着分一下，组一下。
MAC帧在网线上面跑

跨网络传输的本质，是由无数个局域网(子网)转发的结果！
要彻底理解跨网络转发，首先要理解，局域网中报文的转发原理！

以太网帧格式
1.谈一谈局域网转发的原理
  在局域网的主机，呢能不能直接通信呢？ok的。局域网广播。
  看起来一对一通信，实际上大家都知道，只不过和大家没有关系。
  每一台主机都要有唯一的标识符。
    唯一表示符号：每张网卡都要有唯一的一个sn，该网卡的MAC地址，全球范围内具有唯一性

2.认识以太网的MAC报头。a如何分离b如何分用
  IP->以太网MAC
  IP封装MAC帧，MAC报头格式
  目的地址6 源地址6 类型2 数据46-1500 CRC
  
  a.定长分离6+6+2+4=18.
  b.

3.重谈局域网转发的原理(基于协议)
  主机1  主机2  主句3  主机4 主机5
  m1      m2     m3     m4  m5

      主机6  主机7    主机8
      m6     m7        m8
1->8
m8 m1 0800 数据 CRC-->每个主机都接收到，然后开始向上交付。数据链路层主机234567丢弃了
            m8的数据链路层识别到是发给自己的。
所有的主机都接收到了，是自己的才开始交付的。
结论局域网中所有的主机其实都能接收到对应的MAC,只不过大部分主机在自己的数据链路层通过对比
数据帧的目标MAC地址和自己的MAC地址是否相等，来决策要不要在进行后续的处理！

  在具局域网中，网卡有一种模式：混杂模式，不放弃任何数据帧，直接向上交付。局域网抓包工具的原理！
无数的局域网模式。

在局域网当中，任何一个时刻，只能有一个主机正在发消息。
如果多个消息被同时发送，会导致局域网当中的数据发生碰撞，无效数据。
一个局域网一个碰撞域。
1.解决办法。令牌环。
2.以太网
主机的碰撞检查和碰撞避免算法。

1.不高估数据量
2.不低估数据传播的速度
  如果我不断的向局域网发送垃圾数据，不执行碰撞检查和碰撞避免。违法行为。

如何重新看待局域网 -- 临界资源
网络变成了临界资源。碰撞检查+碰撞避免，任何一个时刻只有一个主机能够向临界资源写入数据(临界区)
临牌环是锁。相信光的力量。
  

局域网能不能够很大的，为什么？
不能太大，主机很多，任何时刻发送碰撞的概率增加了。大家都别用了。
交换机
  1.识别局部性的碰撞，对碰撞数据不做转发，不影响别人的数据。减少碰撞域大小的情况。
  2.不对数据正常的数据转发，右侧的碰撞概率也 低了
  碰撞域的大小。
4.以太网的其他问题，重新理解数据转发的整个过程，贯穿协议栈(提出一个问题，引入下一个问题)

question
因为有碰撞域，一台主机发送数据的时候，发送数据量长了好，还是短了好？为什么？




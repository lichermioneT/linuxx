应用软件
操作系统:是一款软件，管理软件硬件。
驱动设备
硬件

基本指令是在做什么事情？就是window的基本操作。
图形化界面 == 命令行。

文件 == 文件属性 + 文件内容属性。
学习操作文件的方法：
操作文件内容
操作文件属性

普通文件和目录文件。
隐藏文件和隐藏目录。ls a显示。
路径分割符号

家目录、根目录、热点目录、上级目录。
/home/xxx
/root

相对路径、绝对路径。

touch创建一个普通文件。
三个时间。
访问时间
修改时间
改变时间

mkdir -p a/b/c/d
parent：创建带父目录的目录

rmdir指令
rmdir == remove directory 删除目录。基本很少用的。

rm指令
rm == remove 删除文件

rm -r(recursive)
















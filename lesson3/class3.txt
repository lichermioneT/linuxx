应用软件
操作系统:是一款软件，管理软件硬件。
驱动设备
硬件

基本指令是在做什么事情？就是window的基本操作。
图形化界面 == 命令行。

文件 == 文件属性 + 文件内容属性。
学习操作文件的方法：
操作文件内容
操作文件属性

普通文件和目录文件。
隐藏文件和隐藏目录。ls a显示。
路径分割符号

家目录、根目录、热点目录、上级目录。
/home/xxx
/root

相对路径、绝对路径。

touch创建一个普通文件。
三个时间。
访问时间
修改时间
改变时间

mkdir -p a/b/c/d
parent：创建带父目录的目录

rmdir指令
rmdir == remove directory 删除目录。基本很少用的。

rm指令
rm == remove 删除文件

rm -r(recursive) 递归删除文件
rm -f(force)强制删除不提示信息
rm -i(interactive)交互式的删除文件，需要确认下一删除。

yum install -y man-pages
man指令
manual pages == man 手册页

1shell指令
2系统调用函数
3c语言库

man:空格向下翻页
man:f向下翻页
man:b向上翻页


echo "hello" > file.txt

cp指令
copy == cp
cp -r(recursive)递归拷贝目录的内容
cp -i(interactivate)交互式拷贝
cp -f(force)


mv指令
move == mv 移动剪切指令
mv file1 file2原地move就是rename
mv dir1 dir2: move移动文件夹不需要携带r
mv file1 file2:改名


tac指令:反过来显示文件的内容。
cat指令:正序来显示文件的内容。
concatenate（连接） == cat

cnt=0; while [ $cnt -le 10000 ]; do echo "hello lic[$cnt]"; let cnt++; done
cnt=0; while [ $cnt -le 10000 ]; do echo "hello lic[$cnt]" >> file; let cnt++; done

cat -n file:文件行号的显示

more: f==空格：向后翻页
more: b:向前翻页
more -n file:默认显示到n行号的内容，然后需要进行翻页的照上面。
/查找的内容


less: f==空格：向后翻页
less: b:向前翻页



